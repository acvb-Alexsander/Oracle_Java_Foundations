String unit;
double value;
void setValue(double newValue){
  value=(isValid(newValue))? newValue:0;
}
double getValue(){
  return (isValid(value)) ? value: 0;
}
boolean isValid(){
  return unit.equals("kg") && value> 0;
}
setValue(-2)
boolean isValid(double value){
  return unit.equals("kg") && value> 0;
}
setValue(-2)
unit="kg";
setValue(-2);
setValue(-2);
setValue(-2)
setValue(10)
getValue()
value
setValue(-2)
value
getValue()